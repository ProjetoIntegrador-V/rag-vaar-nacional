# Copie para .env e preencha. O .env esta no .gitignore.
# NUNCA comite a chave: uma vez no historico do Git, ela vaza mesmo que voce
# apague o arquivo depois.

# ── Qdrant Cloud ──────────────────────────────────────────────────────────
QDRANT_URL=https://SEU-CLUSTER.sa-east-1-0.aws.cloud.qdrant.io
QDRANT_API_KEY=

# ── Modelo de embedding ───────────────────────────────────────────────────
# Trocar o modelo exige REGERAR todos os vetores: vetores de modelos
# diferentes nao sao comparaveis entre si.
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B

# ── LLM da etapa de geracao ───────────────────────────────────────────────
LLM_API_KEY=
