__pycache__/
*.py[cod]
.venv/
venv/
env/

.env
.env.local

# ── Artefatos gerados pelo notebook de embeddings ─────────────────────────
# A colecao e reconstruivel: rode notebooks/02_embeddings_qdrant.ipynb.
data/qdrant/
# Mapa termo -> indice do vetor esparso. Tambem reconstruivel, mas e
# OBRIGATORIO na hora da consulta: sem ele a busca esparsa nao casa nada.
# Se a equipe for compartilhar a colecao sem reprocessar, versione este arquivo.
data/vocabulario_esparso.json

# Cache do HuggingFace (o modelo pesa ~1,2 GB)
.cache/

.vscode/
.idea/
.DS_Store
*.log
.pytest_cache/
